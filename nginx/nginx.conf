events { worker_connections 1024; }
http {
limit_req_zone $binary_remote_addr zone=verifier:10m rate=30r/m;
server {
listen 80;
limit_req zone=verifier burst=10 nodelay;
location / { proxy_pass http://epistemic-verifier:8000; }
location /metrics { proxy_pass http://epistemic-verifier:8000; }
}
}
